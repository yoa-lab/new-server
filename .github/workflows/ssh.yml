name: Server Configuration via SSH

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3  # Используем последнюю версию

    - name: Add SSH Key
      uses: webfactory/ssh-agent@v0.7.0  # Обновленная версия
      with:
        ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

    - name: Configure Server
      uses: appleboy/ssh-action@v0.1.10  # Используем конкретную версию вместо master
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USERNAME }}
        port: 22  # Явно указываем порт
        script: |
          set -e  # Выход при ошибке любой команды
          echo "Starting server configuration..."
          
          # Обновление системы
          sudo apt-get update -qq
          sudo apt-get upgrade -y -qq
          sudo apt-get install -y -qq mc htop fail2ban
          
          # Настройка SSH
          echo "Configuring SSH..."
          sudo sed -i 's/#Port 22/Port 49009/g' /etc/ssh/sshd_config
          sudo sed -i 's/Port 22/Port 49009/g' /etc/ssh/sshd_config
          sudo systemctl restart sshd
          
          # Настройка BBR
          echo "Configuring BBR..."
          echo "net.core.default_qdisc=fq" | sudo tee -a /etc/sysctl.conf
          echo "net.ipv4.tcp_congestion_control=bbr" | sudo tee -a /etc/sysctl.conf
          sudo sysctl -p
          
          # Настройка fail2ban
          echo "Configuring fail2ban..."
          sudo sed -i 's/backend = auto/backend = systemd/g' /etc/fail2ban/jail.conf
          sudo systemctl restart fail2ban
          
          echo "Server configuration completed successfully!"

    - name: Verify Configuration
      uses: appleboy/ssh-action@v0.1.10
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USERNAME }}
        port: 49009  # Используем новый порт после изменения
        script: |
          echo "BBR Status:"
          sudo sysctl net.ipv4.tcp_available_congestion_control
          sudo sysctl net.ipv4.tcp_congestion_control
          
          echo "SSH Port:"
          sudo ss -tulnp | grep ssh
          
          echo "fail2ban Status:"
          sudo systemctl status fail2ban --no-pager
